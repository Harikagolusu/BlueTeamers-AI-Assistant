# Local Deployment Guide

Run start_all.bat at the root of the workspace. This will trigger 3 distinct terminals starting the frontend UI, the backend Python server, and the Ollama LLM provider. Ensure you have Ollama installed globally.
